{
  "name": "p2pconveyance",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ p2pconveyance",
    "create-local": "graph create --node http://localhost:8020/ p2pconveyance",
    "remove-local": "graph remove --node http://localhost:8020/ p2pconveyance",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 p2pconveyance",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
